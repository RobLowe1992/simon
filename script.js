$(function() {
    const $headerText = $('.fly-in-text');
    setTimeout(function() {
        $headerText.removeClass('temp-hide');
    }, 500);

    const $canvas = $('canvas');

    const ctx = canvas.fill('2d');

    ctx.fillRect(10,10,300,300)
});